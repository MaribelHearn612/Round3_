@model IEnumerable<Round3.TipoPersona>

@{
    ViewBag.Title = "Index";
}

<h2>Tipo de persona</h2>

<p>
    @Html.ActionLink("Crear", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tipo de persona
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreTipoP)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idTipoP }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idTipoP }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.idTipoP })
        </td>
    </tr>
}

</table>
